<%= render 'likes/header' %>

<% if @error %>
  <h1><%= @user.first_name %>, you don't have any matches yet.</h1>
<% else %>

  <h3><%= @user.first_name %>, here are all your matches</h3>

  <div class="col-md-offset-1 col-md-3">
  <div class="panel panel-default">
    <div class="panel-heading center">
      <% @matches.each do |match|%>
        <img width="250" height "250" src="<%= match.prof_img %>" class="img-thumbnail">
      <% end %>
    </div>
      <div class="caption">
        <% @matches.each do |match|%>
          <%= match.first_name %>
        
        <br>
          <a href="#" class="btn btn-danger" role="button">Message</a> 
          <a href="/users/<%= match.id %>" class="btn btn-default" role="button">View Profile</a></p>
          <% end %>
      </div>
  </div>
</div>

  <!-- <a href="users/<#%= @user.id %>/likes/new">Find New Matches</a> -->
<% end %>




